<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <ImageButton
        android:id="@+id/btnConfigureServer"
        style="?android:attr/buttonBarButtonStyle"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:contentDescription="@string/btn_play_files"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:src="@drawable/ic_action_settings" />

    <Switch
        android:id="@+id/selectServerSwitch"
        style="?android:attr/buttonBarButtonStyle"
        android:layout_toLeftOf="@id/btnConfigureServer"
        android:layout_toStartOf="@id/btnConfigureServer"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"/>

    <TextView
        android:id="@+id/tvServerItem"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_toLeftOf="@id/selectServerSwitch"
        android:layout_toStartOf="@id/selectServerSwitch"
        android:padding="@dimen/standard_textview_padding"
        android:text=""
        android:ellipsize="end"
        android:singleLine="true"
        android:textAppearance="?android:attr/textAppearanceLarge" />
	
</RelativeLayout>